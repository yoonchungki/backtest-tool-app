# backtest-tool-app

한국투자증권(KIS) 데이터로 돌리는 주식 백테스트 도구. 원래 로컬 `backtest_tool_N.html`(버전 번호 올려가며 저장하던 파일, `C:\Users\yck84\Downloads\`)로 관리하던 걸, 모바일에서 파일 버전 관리가 번거로워서 Vercel 웹앱으로 배포함.

- **저장소**: `https://github.com/yoonchungki/backtest-tool-app` (Private)
- **배포**: Vercel, `https://backtest-tool-app.vercel.app` (git push → 1~2분 내 자동 배포)
- **로컬 경로**: `C:\Users\yck84\backtest-tool-app`
- **로컬 버전 파일과의 관계**: `backtest_tool_N.html`(Downloads 폴더, 최신 `_23`)은 별개로 계속 관리 중. 이 repo의 `index.html`은 그 파일을 복사해서 출발했지만, 지금은 이 repo 쪽에서만 git commit으로 버전 관리하고 있고 두 파일이 자동 동기화되지는 않음.

## 파일 구조
- `index.html` — 프론트 전체(단일 파일, HTML+CSS+JS, 내장 데이터셋 포함이라 큼(5MB+) — Read 툴로 통째로 못 열림, 큰 상수를 볼 땐 Read도 실패할 수 있으니 Python으로 `content.index('const X')` 찾아서 슬라이스하는 식으로 볼 것, 편집은 offset/limit Read나 짧은 고유 앵커 문자열로 Edit). `PRESETS`(탭1/탭2용, KIS 원주가 OHLCV)와 `US_ETF_DATA`/`USD_KRW_DATA`(탭3 전용, Yahoo Finance 수정종가+환율)는 완전히 분리된 별개 상수 — 아래 "9." 참고.
- `api/history.py` — KIS 시세/시가총액 조회 서버리스 함수 (`/api/history`)
- `api/presets.py` — 여러 기기 동기화용 저장소 (`/api/presets`, Vercel Blob)
- `api/usetf.py` — 탭3 미국ETF/환율 시세 조회 서버리스 함수(야후 파이낸스 프록시, `/api/usetf`) — 아래 "11." 참고
- `manifest.json` — PWA 홈화면 추가용
- `vercel.json` — builds/routes 설정 (Python 함수 + 정적 파일 라우팅 분리)

## 환경변수 (Vercel)
`KIS_APP_KEY`, `KIS_APP_SECRET`, `KIS_MODE`(paper/live), `BLOB_READ_WRITE_TOKEN`(Vercel Blob, 스토어 생성 시 "Add a read-write token env var" 체크 필요 — 기본 꺼져있어서 놓치기 쉬움). 계좌 조회는 안 하므로 `KIS_CANO`/`KIS_ACNT_PRDT_CD`는 불필요.

## 탭 구성 (2026-09-03 이후 화면 표시 번호 1~5, 내부 id는 그대로 — 표시 번호와 내부 id가 일치하지 않는 탭이 있음)
1. **변동성 돌파 전략** (`tab1`) — 단일 종목, k값 그리드 스윕. 초기투자금 조절 가능, 항상 정수 주 단위로 매매(소수점/분수 주 매매 옵션은 2026-08-16에 제거), 결과표 컬럼별(CAGR/MDD/칼마비율 등 11개) 범위 필터, 슬리피지(틱/%) 설정, 차트 MDD·로그스케일 토글.
2. **변동성전략 (여러종목투자)** (`tab1b`, 예전 "1-1") — 여러 종목을 각자 k값으로 동시 운용. 계좌는 하나의 현금 풀을 공유(목표비중 강제 리밸런싱 없음 — 아래 "4." 참고). 종목 선택+k값 세팅을 이름 붙여 저장/불러오기(`mvStrategyPresets`). 탭1과 동일하게 슬리피지(틱/%) 설정, 차트 MDD·로그스케일 토글 있음(2026-08-16 추가), 종목별 슬리피지(%)도 추가됨(2026-09-02, 아래 "7." 참고).
3. **개인연금 보유전략** (`tabPension`, 2026-09-03 신설) — k값 신호 없이 여러 종목을 목표비중대로 매수해서 보유하다가 N개월마다 목표비중으로 리밸런싱하는 정적 자산배분 전략. 종목 선택 시 비중 자동 균등분배, 리밸런싱 간격을 탭1 k값처럼 최소/최대/step 그리드서치로 여러 조합 한 번에 비교 가능. 화면 구성(그래프/거래이력)은 탭2(여러종목투자)를 그대로 본떠 만듦. "미국ETF비교" 체크박스로 국내 상장 기간이 짧은 종목을 추종 미국 ETF×환율 환산값으로 바꿔서 더 긴 기간 백테스트 가능(아래 "9." 참고). 아래 "8." 참고.
4. **섹터ETF 매매전략** (`tab2`) — 거래대금 상위 종목 로테이션.
5. **상관관계 분석** (`tab3`) — 2종목이면 산점도, 3종목 이상이면 상관계수 매트릭스.

(2026-08-13에 구 탭4 "연금 적립+눌림목 매수"와 구 탭5 "systrader79 자산배분"은 안 쓴다고 해서 완전 삭제함 — 2026-09-03에 새로 만든 "개인연금 보유전략"은 이것들과 이름만 비슷할 뿐 완전히 새로 설계한 훨씬 단순한 기능임, 옛 코드 재사용 없음.)

## 핵심 설계 패턴 & 주의사항

### 1. Vercel Blob 동기화 — "병합만, 절대 덮어쓰지 않음" + 삭제는 별도 tombstone 필요
`localStorage`(기기별) + `/api/presets`(Vercel Blob, 기기 간 공유) 이중 저장 구조. `applyOverrideData()`가 서버/로컬 데이터를 항상 **병합**(`Object.assign`)해서 상태를 만듦 — 한 기기가 아직 다른 기기의 데이터를 다 모르는 상태에서 저장해도 기존 데이터를 지우지 않기 위함.

**중요한 함정**: 병합 로직(`Object.assign`)은 키를 추가/덮어쓸 수만 있지 **절대 제거를 표현 못 함**. "삭제" 기능을 만들 때마다 `deletedCodes`(종목 삭제), `deletedMvStrategyNames`(전략 프리셋 삭제)처럼 **별도의 tombstone Set**을 만들어서, 병합 직후 그 목록에 있는 항목을 다시 한번 강제로 지워내야 함. 앞으로 이 구조에 새로운 "저장 가능한 목록" 필드를 추가하고 거기에 삭제 기능도 필요하다면, 처음부터 tombstone 세트를 같이 설계할 것.

**tombstone의 반대쪽 함정(2026-08-17에 발견)**: tombstone Set에 넣기만 하고 **빼는 로직을 빠뜨리기 쉬움**. `deletedCodes`는 `deleteStocks()`에서 `.add()`만 있고 어디서도 `.delete()`를 안 했었음 — 그래서 한 번 삭제한 종목코드를 "새 종목 추가"로 나중에 똑같은 코드로 다시 추가해도, 그 코드가 여전히 `deletedCodes`에 남아있어서 다음 동기화/새로고침 때 `applyOverrideData()`가 또 걸러내서 사라졌음(당장은 화면에 보이다가 새로고침하면 사라지는 패턴이라 원인 파악이 헷갈리기 쉬움). `dataAddBtn`에서 코드를 성공적으로 다시 추가할 때 `deletedCodes.delete(code)`를 호출하도록 수정(`전략` 쪽은 재저장 시 `deletedMvStrategyNames.delete(name)`으로 이미 처리돼있었음). **tombstone Set을 쓰는 모든 곳은 "추가" 동작이 새로 생길 때마다 그 tombstone에서 빼는 것도 같이 챙길 것.**

저장이 걸리는 모든 동작(`deleteStocks`, `dataUpdateAllBtn`, `dataAddBtn`, 전략 프리셋 저장/삭제) 앞에는 항상 `await syncPresetOverridesFromServer()`를 먼저 호출해서 서버 최신본을 받아온 뒤에 저장함 — 이게 없으면 뒤처진 기기가 다른 기기의 데이터를 덮어써버림.

**세 번째 변종(2026-08-18에 발견) — "다른 기기"의 오래된 로컬 tombstone이 영원히 안 풀림**: 앞의 두 함정은 "같은 기기 안에서 삭제→재추가"였는데, 이번엔 **기기가 서로 다른** 경우였음. 폰에서 예전에 종목을 삭제한 적이 있고, 그 뒤 PC에서 그 종목을 재추가하면 PC의 `deletedCodes.delete(code)`로 PC 자신과 서버는 깨끗해지지만, **폰의 localStorage에 남아있는 옛 tombstone은 그대로**임 — `applyOverrideData()`가 서버 `deletedCodes`를 로컬과 union(추가)만 하고 절대 빼질 않아서, 폰은 서버 동기화를 아무리 받아도 그 종목이 계속 숨겨져 있었음(PC엔 28종목 보이는데 폰엔 20종목만 보이는 식으로 발견됨). **해결**: `applyOverrideData()`에서 서버 `infoList`에는 있는데 서버 `deletedCodes`엔 없는 코드는, 그 즉시 로컬 tombstone에서도 강제로 빼도록 함(서버의 "지금 삭제 상태"를 항상 신뢰). `mvStrategyPresets`/`deletedMvStrategyNames`도 동일 패턴이라 같이 고침. **교훈**: tombstone Set은 "재추가하는 그 기기"만 지우면 끝나는 게 아니라, **동기화를 받는 모든 기기가 서버의 최신 tombstone 목록을 기준으로 자기 로컬 tombstone도 줄일 수 있어야** 진짜로 해결됨 — union-only 병합은 삭제 자체는 잘 전파하지만 "삭제 취소"는 절대 전파를 못 함.

**다섯 번째 변종(2026-09-05에 발견) — 삭제 버튼 자체가 sync 순서 때문에 자기 자신을 되살림**: 앞의 네 변종은 전부 "다른 기기"나 "예전에" 생긴 tombstone 문제였는데, 이번엔 **같은 기기에서 지금 막 누른 삭제 버튼이 그 자리에서 바로 실패**하는 문제였음. `mvStrategyDeleteBtn`/`penStrategyDeleteBtn` 클릭 핸들러가 `deletedMvStrategyNames.add(name) → delete mvStrategyPresets[name] → await syncPresetOverridesFromServer() → delete mvStrategyPresets[name](재시도) → savePresetOverrides([])` 순서로 짜여있었음 — **삭제 표시를 먼저 하고 나서 서버 sync를 함**. 그런데 서버는 아직 이 삭제를 모르는 상태(`deletedMvStrategyNames`에 없음)라, `syncPresetOverridesFromServer()`가 부르는 `applyOverrideData()`의 "세 번째 변종" 화해 로직(`saved.mvStrategyPresets`엔 있는데 `saved.deletedMvStrategyNames`엔 없는 이름은 로컬 tombstone에서 뺌)이 **방금 이 기기에서 막 삭제한 것을 "다른 기기가 되살린 것"으로 착각**해서 로컬 tombstone에서 그 이름을 도로 빼버리고, `mvStrategyPresets`도 서버 응답으로 덮어써서 되살아남. 그 뒤에 오는 두 번째 `delete mvStrategyPresets[name]`은 객체는 다시 지우지만 tombstone Set엔 이름이 없는 상태라, `savePresetOverrides()`가 "삭제된 전략은 병합 후에도 다시 제거"하는 필터링(`deletedMvStrategyNames.forEach(name => delete saved.mvStrategyPresets[name])`)이 이 이름을 걸러내지 못하고 그대로 서버에 다시 저장되며, 화면엔 삭제됐다가 다음 렌더링/새로고침 때 다시 나타남. **해결**: sync를 항상 먼저 하고, tombstone 추가/객체 삭제는 sync가 끝난 뒤 딱 한 번만 하도록 순서를 바꿈(`dataUpdateAllBtn` 등 다른 "저장 전 sync" 동작들과 동일한 순서로 통일) — `deletedCodes`(종목 삭제) 쪽은 원래부터 이 순서였어서 문제가 없었고, 전략 삭제 두 곳만 순서가 반대였음. **교훈**: "저장 전엔 항상 먼저 sync" 원칙은 sync가 **아직 반영 안 된 로컬 변경사항을 서버 데이터로 덮어쓸 수 있다**는 뜻이기도 함 — tombstone처럼 "지금 막 바뀐 로컬 상태"를 다루는 동작은 반드시 sync가 끝난 *다음에* 반영해야 함.

**저장된 전략 이름 변경(2026-09-06 추가)**: 삭제(×) 버튼 옆에 이름변경(✎) 버튼을 탭2/탭3 둘 다 추가함. 내부적으로는 "새 이름으로 추가 + 옛 이름 삭제"라 다섯 번째 변종과 완전히 같은 함정이 적용됨 — 그래서 삭제와 동일하게 **sync 먼저 → `presets[새이름]=presets[옛이름]; delete presets[옛이름]; deletedNames.add(옛이름)` → save** 순서로 만듦. 새 이름이 기존 이름과 겹치면 확인 후 덮어쓰기(다른 항목을 몰래 지우는 게 아니라 사용자가 명시적으로 동의한 경우만), `prompt()` 취소나 같은 이름 입력은 아무 것도 안 함.

**저장된 전략 순서 변경 + 탭 전환 시 맨 앞 전략 자동 로드(2026-09-06 추가)**: ▲▼ 버튼을 이름변경 버튼 왼쪽에 추가(stock-signal-app 전략1 카드의 `STRATEGY1_ORDER_KEY` 패턴을 그대로 가져옴). 순서 자체는 **이 기기 `localStorage`에만 저장**하고 Vercel Blob 동기화 대상이 아님(취향 문제라 기기마다 달라도 되고, 다른 저장 데이터처럼 tombstone 화해까지 만들 필요가 없음) — `MV_STRATEGY_ORDER_KEY`/`PEN_STRATEGY_ORDER_KEY`에 이름 배열로 저장, `applyMvStrategyOrder()`/`applyPenStrategyOrder()`가 그 배열 순서대로 정렬하고 기록에 없는(새로 저장된) 이름은 알파벳순으로 뒤에 붙임. 이름변경 시 순서 기록에서도 옛 이름→새 이름으로 같이 바꿔줘야 함(안 그러면 이름 바뀐 순간 "새 이름"으로 취급돼서 맨 뒤로 밀림) — 삭제 시엔 순서 기록에서도 빼줌(안 빼도 동작엔 문제없지만 깔끔하게).

탭 전환 핸들러(`.tabBtn` 클릭)에서 `tab1b`/`tabPension`으로 이동할 때마다 `autoApplyFirstMvStrategy()`/`autoApplyFirstPenStrategy()`를 호출해서, 그 시점 순서 기준 맨 앞 전략을 자동으로 불러옴(`applyMvStrategySnapshot`/`applyPenStrategySnapshot` 재사용) — 매번 목록에서 클릭 안 해도 되게 함. 저장된 전략이 하나도 없으면 아무 것도 안 함. 초기 페이지 로드 시(기본 활성 탭은 탭1)는 트리거 안 되고, 사용자가 실제로 그 탭 버튼을 눌러 전환할 때만 동작함.

**네 번째 변종(2026-09-04에 발견) — "세 번째 변종"의 화해 로직이 기본 내장 종목(`BASE_PRESET_CODES`)은 아예 다루지 않음**: `saved.infoList`는 `savePresetOverrides()`가 저장할 때부터 `PRESET_INFO_LIST.filter(p => !BASE_PRESET_CODES.has(p.code))`로 기본 내장 종목을 의도적으로 걸러내고 저장함(기본 내장 종목은 코드 자체에 박혀있어서 굳이 동기화할 필요가 없다는 설계) — 그런데 세 번째 변종의 화해 로직(`saved.infoList.forEach(...)`)이 딱 이 `saved.infoList`만 훑어서 로컬 tombstone을 지우다 보니, **기본 내장 종목의 tombstone은 애초에 화해 대상에 들지도 못했음**. 그래서 어느 기기에서 기본 내장 종목을 한 번 삭제한 적이 있으면, 서버 `deletedCodes`에선 그 코드가 이미 빠졌어도 그 기기의 로컬 tombstone은 세 번째 변종 수정 이후로도 계속 영원히 안 풀렸음(사용자가 PC에서 추가한 "KODEX 200미국채혼합(284430)"이 폰에는 계속 안 보인다고 알려줘서 발견 — 실제로 서버 점검해보니 `presets['284430']`엔 KIS로 최신화한 시세가 있는데 `infoList`/`deletedCodes` 어디에도 284430이 없었음, 즉 284430은 기본 내장 종목이었고 폰의 로컬 tombstone만 문제였음). **해결**: `applyOverrideData()`에서 `BASE_PRESET_CODES`도 서버 `deletedCodes` 기준으로 별도로 화해하도록 추가(`saved.infoList` 유무와 무관하게 항상 실행). 추가로, tombstone을 지우는 것만으론 부족함 — 예전에 이미 `PRESET_INFO_LIST`에서 스플라이스로 빠져나간 상태라면 그 배열엔 되돌릴 방법이 없어서(`saved.infoList` 병합 경로는 기본 내장 종목을 아예 안 다룸), 페이지 로드 시점에 만들어둔 원본 스냅샷 `BASE_PRESET_INFO_BY_CODE`(코드→`{code,name}` 원본 객체)에서 다시 꺼내 `PRESET_INFO_LIST.push()`로 복원함. **교훈**: "동기화 대상에서 의도적으로 제외한 것"(기본 내장 종목)과 "tombstone 화해 대상에서 제외되는 것"은 서로 다른 개념인데 같은 필터(`!BASE_PRESET_CODES.has`)를 공유하다 보니 후자까지 덩달아 빠졌음 — 앞으로 이런 "항상 존재해야 하는 기본값" 종류의 데이터에 tombstone 개념을 적용할 땐, 화해 로직이 그 데이터의 존재 여부와 무관하게 항상 돌게 해야 함.

### 2. KIS 토큰 재발급 제한 (1분에 1회) — 서버리스 함수는 in-memory 캐시를 요청 간에 공유한다고 가정하지 말 것
`get_access_token()`의 `_token_cache`는 모듈 전역 dict라 같은 컨테이너가 재사용(warm start)될 때만 도움됨 — Vercel이 요청마다 다른 인스턴스로 라우팅하면 캐시가 안 먹힘. 클라이언트가 종목마다 별도 HTTP 요청을 반복하면(예: "전체 종목 최신화"가 종목 수만큼 시가총액 요청) 최악의 경우 요청 수만큼 토큰을 새로 받으려다 KIS의 "같은 앱키로 1분 안에 재발급 금지" 제한에 걸림.

**해결 패턴**: 여러 항목을 처리해야 하면 **서버 함수 한 번의 실행 안에서 토큰을 한 번만 받아 순회 처리**하는 배치 엔드포인트로 묶을 것. `api/history.py`의 `marketcap_batch` 모드(`?marketcap_batch=CODE1,CODE2,...`)가 이 패턴 — Vercel 실행시간 제한(기본 10초) 대비 `time_budget_sec` 넘기면 처리 못한 나머지를 `remaining`으로 돌려주고, 프론트(`fetchMarketCapBatch()`)가 remaining만 이어서 재요청(정상적으론 1~2회 왕복). 자매 프로젝트 stock-signal-app에서도 동일한 문제를 겪었음 (`CLAUDE.md` 참고).

### 3. localStorage 용량 초과가 서버 저장까지 막지 않게
`savePresetOverrides()`에서 `localStorage.setItem()`과 서버 POST(`fetch('/api/presets', ...)`)는 별도 try/catch — 로컬 저장이 용량 초과로 실패해도 서버 저장은 항상 별도로 시도함(과거엔 같은 try 블록이라 로컬 실패가 서버 저장까지 막아서 방금 받은 데이터가 새로고침하면 사라지는 사고가 있었음).

### 4. 탭2(여러종목투자)는 목표비중 강제 리밸런싱이 아니라 "공유 현금 풀" 모델
2026-08-15에 구조를 바꿈 — 예전엔 리밸런싱 시점마다(매 거래마다/N일 주기) 전체 종목을 지정한 목표비중(%)으로 강제로 맞췄는데, 정수 주 단위로는 애초에 정확한 %를 못 맞추고("8종목 중 2종목만 매도했는데 12.5%가 정확히 나올 수 없음"), 이미 보유 중인 종목까지 억지로 사고팔아서 실제 계좌로 거래한 것과 비교하기엔 비현실적이었음(탭2를 실거래 내역 검증용으로 쓰려던 사용자 요청으로 발견됨).

**지금 구조**: 종목별 초기투자금/목표비중 입력 자체가 없어짐 — 계좌 전체가 `simulateMvPortfolio()` 안의 `cash` 변수 하나(공유 현금 풀)를 씀. 하루 처리 순서: ①보유 중인 종목의 매도 신호부터 처리(대금은 공유 현금으로 합쳐짐, `soldToday` Set에 기록) → ②그 시점에 매수 대기 중(미보유, **오늘 방금 매도한 종목은 제외**)인 종목 수로 공유 현금을 균등하게 나눈 게 그날의 "배정예산"(perBudget) → ③매수 신호가 난 종목만 그 예산 안에서 살 수 있는 최대 정수 주를 매수, 남는 돈은 그대로 현금으로 남음. `soldToday` 제외 처리가 없으면 오늘 판 종목이 같은 날 바로 재매수 후보에 들어가서 하루에 매도+매수 2건이 찍히는 문제가 있었음(2026-08-15에 발견/수정) — 종목당 하루 최대 1건(매수 또는 매도)만 나오도록 강제함. `stock-signal-app`의 "배정예산 = 주문가능금액 ÷ 미보유종목수, 균등분배" 로직과 동일한 패턴 — 여러 프로젝트에서 "실제 계좌처럼 자금을 배분"해야 할 때 재사용할 수 있는 접근.

"보유 전략(기준)"(`computeMvBase`)도 같은 철학으로 총초기투자금을 종목 수로 균등 분배해서 매수 후 계속 보유하는 걸로 통일함(개별 목표비중 개념 자체가 없어졌으므로).

**2026-09-10: 종목별 "비중(가중치)" 추가 — 균등분배는 기본값일 뿐, 강제 아님.** 칼마비율을 더 올릴 방법을 찾다가, 종목별 개별 최적화 결과(탭1)를 보면 칼마비율 편차가 큰데(예: 어떤 종목은 7 이상, 어떤 종목은 2 미만) 탭2는 매수후보들끼리 무조건 균등분배해서 이 차이를 반영 못 하고 있었음 — 그래서 `mvStockParams[code].weight`(기본값 1)를 추가하고, "②배정예산 계산" 단계를 `cashBeforeToday / 후보수`가 아니라 **그날 매수후보들의 weight 합 대비 각자 weight 비율**로 나누도록 바꿈(`perBudget = cashBeforeToday * (weight_e / 그날 후보들의 weight 합)`). **weight를 전부 1(기본값)로 두면 이전과 완전히 동일한 균등분배** — 그래서 옛날에 저장된 전략(weight 필드가 아예 없음)을 불러와도 자동으로 균등분배로 채워짐(`?? 1` 폴백, `renderMvStockParamsTable()`/`simulateMvPortfolio()`/`computeMvBase()` 세 군데 다 동일). **weight=0은 그 종목을 매수 후보에서 사실상 제외**하는 효과(배정예산이 0이 돼서 `affordable<=0`으로 걸러짐) — 종목을 빼지 않고 그대로 목록엔 두면서 비중만 0으로 두는 것도 가능. `computeMvBase()`(보유 전략 기준)도 동일한 weight로 초기 배분해서 전략과 기준이 계속 1:1 비교 가능하게 함. weight는 `mvStockParams[code]` 객체 안에 있는 필드라 저장/불러오기(`collectMvStrategySnapshot`/`applyMvStrategySnapshot`)에 별도 코드 없이 자동으로 같이 저장/복원됨. UI는 "종목별 설정" 표에 열이 하나 추가된 것(입력값 옆에 실시간 비중% 표시) — 타이핑 중 매번 표 전체를 다시 그리면 입력 포커스가 날아가서, weight 변경 시엔 %표시(`.mvWeightPct` span)만 갱신하고 입력창 자체는 안 건드림.

**2026-09-11: "전체기간"/"일부 종목만 있던 시기" 백테스트용 — 새 종목 편입 시 그 시점 총자산을 강제로 재분배.** 사용자가 "겹쳐진기간" 대신 "전체기간"(종목마다 상장일이 달라서 masterDates가 합집합)으로 더 긴 기간 검증을 해보다가 발견. 이 문제는 두 단계로 수정됨 — 처음엔 "종목별 슬롯 자금을 그 종목 상장일에 새로 투입"(총자본을 처음부터 안 풀고, 새 종목 생길 때마다 그 몫만 추가 투입)으로 고쳤는데, 사용자가 실제 그래프를 보고 "그게 아니라 둘 다 총자본 전액으로 시작하고, 새 종목 생기면 그때까지의 평가금액을 지금 활성 종목 수 기준으로 재분배해야 한다"고 정정함 — 처음 요청을 오해했던 것. **최종 방식**: `cash`는 여전히 t=0에 `totalInitCap` 전액으로 시작(그 시점 활성 종목이 1개뿐이면 그 종목이 전액을 가짐). 이후 새 종목이 처음 등장하는 날(`everSeen[e]`로 추적), 그때까지 보유 중이던(자기 신호로는 아직 안 팔린) 다른 종목들을 **전부 그날 종가로 강제 청산**(`simulateMvPortfolio()`의 "1-1단계", `tradeLog`에 `side:'강제매도(신규종목편입)'`로 남음) — 청산된 현금은 강제로 재매수하지 않고, 그날 이후 평소의 비중기반 배정예산 로직(2단계)을 그대로 타고 각자 k신호가 나는 대로 자연스럽게 재매수됨(매도만 강제, 매수는 여전히 신호 기반 — 탭2 핵심 원칙 절반은 유지). `computeMvBase()`(보유 전략 기준)도 동일한 철학으로 다시 짬 — 다만 이쪽은 "항상 100% 투자 상태"가 정의라서, 새 종목 편입 시 그 시점 평가금액을 활성 종목들에 비중대로 즉시 재매수함(청산 즉시 재매수, 대기 없음). 매도(강제청산 포함)는 항상 `cashBeforeToday` 스냅샷 이전이 아니라 이후에 일어나므로, 그 현금은 실제 신호 매도와 마찬가지로 다음날부터 배정예산에 반영됨(위 "전날 마감 현금 기준" 문단과 동일 원칙). **겹쳐진기간(overlap) 모드에선 모든 종목이 masterDates[0]부터 데이터가 있어서 새 종목 편입 이벤트 자체가 한 번도 안 일어남 → 예전 동작과 완전히 동일** — 실거래 검증에 쓰는 기본 모드는 결과값 영향 전혀 없음(회귀 테스트로 확인, 균등비중 3종목 시나리오에서 배정예산 수치 완전히 일치). "전체기간" 모드를 쓸 때만 이 로직이 발동함.

**배정예산은 반드시 "전날 마감 현금" 기준(2026-08-25에 발견/수정, `cashBeforeToday`)**: 사용자는 실제로 매수/매도 주문을 전날 밤에 예약주문으로 미리 걸어두는 방식이라, 그날 매도가 체결돼서 들어올 대금은 그날 매수 주문 시점엔 아직 없는 돈임. `simulateMvPortfolio()`의 하루 처리 순서(①매도 처리→②배정예산 계산→③매수 처리) 자체는 유지하되, **배정예산(`perBudget`)은 그날 매도 처리 전에 스냅샷해둔 `cashBeforeToday`로 나눌 것** — 매도 처리 후의 `cash`로 나누면 신호 없는 종목의 유휴 현금이 그날 우연히 매수 후보가 적은 날에 몰려서 배정예산이 정상 슬롯(총자금÷종목수) 대비 비정상적으로 커짐(실사용자가 거래이력에서 발견, 커밋 `3d69397`). `cash` 변수 자체(매도대금 반영)는 계속 누적해서 다음날 배정예산 계산에 쓰임 — 그러니 자산 총액(`curve[t]`) 계산에는 영향 없음, 배정예산 계산식만 바뀐 것.

### 5. 과거 시세: 미수정주가(원주가) 사용 (`api/history.py`의 `FID_ORG_ADJ_PRC`)
2026-08-16에 `FID_ORG_ADJ_PRC`를 `"0"`(수정주가)에서 `"1"`(원주가/미수정주가)로 변경함. KIS 공식 문서 기준 "0"=수정주가(액면분할 등 반영), "1"=원주가. 이 백테스트 엔진은 분할 이벤트가 생겼을 때 보유수량을 자동으로 조정하는 로직이 없어서, 분할이 있었던 종목을 수정주가로 백테스트하면 오히려 그 구간 결과가 왜곡됨 — 이 앱 유니버스가 대부분 ETF(분할이 드묾)라 미수정주가가 실제 체결가에 더 가깝다고 판단해서 전환함. **주의**: 이미 저장돼있던 과거 데이터(로컬 localStorage + Vercel Blob 캐시)는 이 변경과 무관하게 예전(수정주가) 값이 그대로 남아있음 — "전체 종목 최신화"를 눌러야 새로 받아오는 구간부터 미수정주가로 바뀜(과거 이미 저장된 구간은 그 버튼만으로는 안 바뀌고, 그 종목을 삭제 후 재추가해야 완전히 새로 받아짐). 앞으로 액면분할 이력이 있는 종목을 다루게 되면 이 결정을 재검토할 것.

### 6. 로컬 브라우저 테스트 방법
- `index.html`이 커서(4MB+) Claude Browser의 `file://` navigate가 가끔 불안정함(멈추거나 "the user declined access"). → 스크래치패드로 복사 후 `python -m http.server PORT --directory <scratchpad>`로 띄우고 `http://127.0.0.1:PORT/파일명`으로 접속.
- KIS API/Vercel Blob 없이 로직만 검증할 땐 `window.fetch`를 몽키패치해서 `/api/presets`, `/api/history?...` 응답을 mock으로 대체 (과거 세션들에서 반복 사용한 패턴, 필요하면 새로 짜면 됨).
- 새 기능은 전체 파일을 브라우저에 다 로드하기보다, 관련 함수만 `javascript_exec`로 직접 호출해서 목업 데이터로 먼저 검증하는 게 빠르고 안정적임.
- 테스트 끝나면 스크래치패드 사본과 로컬 서버 프로세스는 정리할 것.

### 7. 탭2(여러종목투자) 종목별 슬리피지(%) — "목표가 추가수정"과는 다른 별개 기능
2026-09-02 추가. 탭1/탭2에 원래 있던 "목표가 추가수정(x)"(탭1) / "슬리피지 적용"(탭2)은 **목표가 자체를 밀어버리는** 방식이라 모든 종목에 동일하게 적용됨. 반면 이 종목별 슬리피지(%)는 **목표가/매매 트리거는 그대로 두고, 체결 시 수수료처럼 종목별로 다른 비율만큼 추가로 빠지는** 방식(`simulateMvPortfolio()`에서 `fee` 대신 `fee + pm.slipPct/100`). stock-signal-app 실거래 내역과 대조해보니 종목마다 목표가-체결가 괴리 비율이 달라서, 기존의 "전종목 동일 적용" 방식으로는 재현이 안 돼서 추가함. `mvStockParams[code].slipPct`에 저장, 프리셋 저장/복원은 기존 스프레드 방식(`{...mvStockParams[code]}`)이라 자동으로 포함됨(별도 마이그레이션 코드 불필요). 탭1엔 아직 없음(탭2만 요청받아서 만듦).

### 8. 탭3(개인연금 보유전략) 설계 — k값 신호 없는 정적 자산배분
2026-09-03 신설. 다른 탭들과 근본적으로 다른 종류의 전략: **당일시가±k×전일변동폭** 같은 매일의 매매 신호가 없고, 그냥 t=0에 목표비중대로 매수해서 들고 있다가 N개월마다 그 시점 평가금액×목표비중으로 다시 맞추는(리밸런싱) 정적 배분 전략임. 그래서 탭1/탭2의 `larryWilliams`/`simulateMvPortfolio`와는 별개의 새 함수 `simulatePensionPortfolio()`로 만듦(k값·이평필터·틱슬리피지 등 신호 관련 파라미터가 아예 없음).

- **리밸런싱 날짜 계산**: 기존에 연도별 통계용으로 있던 `buildPeriodBoundariesGeneric(dates, monthsPerPeriod)`를 그대로 재사용 — 각 구간의 시작 인덱스(첫 구간 제외)가 리밸런싱 시점. 새 유틸 함수를 따로 안 만들고 기존 걸 용도 변경 없이 그대로 씀.
- **하루 처리 순서**: 탭2(`simulateMvPortfolio`)와 동일하게 "목표보다 많이 든 종목 매도(현금 확보) → 그 현금으로 목표보다 적게 든 종목 매수" 순서. 아직 상장 전(그날 시세 없음)인 종목은 그 리밸런싱 회차에서 제외하고 나머지 종목끼리 비중을 정규화(wSum으로 나눔)해서 재분배함.
- **realizedPnl 추적**: 탭2는 종목이 완전히 flat→long→flat(전량 매도)될 때만 손익을 확정하는데, 리밸런싱은 부분매도/부분매수가 반복돼서 그 방식이 안 맞음 — 대신 `costBasis`(보유분 평균매입원가 총액)를 매도 비율만큼 비례 차감하는 방식으로 종목별 realizedPnl을 근사함. 승률/손익비/TPI 같은 "라운드트립" 통계는 이 구조에 안 맞아서 `statsFromCurveCustomBase()`에 빈 tradeList(`[]`)를 넘김 — 탭2가 "보유 전략(기준)" 비교용 커브에 쓰는 것과 동일한, 이미 검증된 안전한 패턴(승률 등 칸은 그냥 "-"로 표시됨).
- **비교 기준(base) 커브**: `computePenBase()` — 리밸런싱 없이 t=0에 목표비중대로 매수해서 그대로 계속 보유(매매 전혀 없음). "리밸런싱이 실제로 도움이 됐는지"를 1:1로 보여주는 게 목적이라, 탭2의 `computeMvBase()`(균등분배 매수 후 보유)와 개념은 같고 가중치만 다름.
- **화면 구성은 탭2를 그대로 복제**: 사용자가 "그래프나 거래내역은 탭2 참고해서 동일하게" 요청 — 차트(MDD·로그스케일 토글 포함)/연도별 성과표/거래이력 표/엑셀 다운로드 렌더링 함수를 `mv` 접두어와 동일한 구조로 `pen` 접두어를 붙여 그대로 복제함(코드 공유 리팩터링은 안 함 — 탭1/탭1b/섹터ETF도 각자 독립적인 렌더 함수를 갖고 있는 이 프로젝트의 기존 관례를 따름).
- **저장된 전략(`penStrategyPresets`/`deletedPenStrategyNames`)**: `mvStrategyPresets`와 완전히 동일한 병합+tombstone 패턴으로 `applyOverrideData`/`savePresetOverrides`/`syncPresetOverridesFromServer` 세 곳에 나란히 추가함 — 앞으로 4번째 탭이 저장 가능한 전략을 필요로 하게 되면 이 세 함수에 동일한 패턴을 또 추가할 것(리팩터링해서 공통화하는 게 나을 수도 있음, 아직은 탭 3개뿐이라 안 함).
- **MVP 범위로 일부러 뺀 것들**(사용자가 "일단 만든 후 추가로 수정"이라고 명시적으로 허용함): 엑셀 추가 업로드(내장 데이터셋 선택만 가능), 이동평균 국면 필터, 틱/% 슬리피지 설정. 필요해지면 탭2의 해당 UI/로직을 그대로 옮겨오면 됨.
- **테스트**: 로컬 브라우저에서 `javascript_exec`로 `penRunBtn.click()` 후 결과 직접 검사, 프리셋 저장→값 변경→불러오기 왕복까지 확인함(async 클릭 핸들러라 테스트 시 `await new Promise(r=>setTimeout(r,300))`로 기다려야 함 — 안 그러면 저장이 아직 안 끝난 상태를 검사하게 됨, 실제로 이 세션에서 한 번 겪음).

**2026-09-03 후속 수정 2건** (사용자가 초판 써보고 바로 요청):
1. **종목 선택 시 자동 균등분배** — 체크박스 change 이벤트마다 `equalizePenWeights()`가 현재 선택된 종목 전부를 100/N%로 강제 재분배함(수동으로 만져둔 값도 이때 덮어씀 — "선택하면 자동으로 나뉘게"라는 요청 그대로). 개별 칸을 직접 고치는 건 그대로 유지됨(다음 선택 변경 전까지).
2. **리밸런싱 간격도 탭1 k값처럼 그리드서치** — `penRebalanceMonths` 단일 입력을 `penRebalanceMin`/`Max`/`Step` 3개로 교체하고, 기존 범용 `range()` 함수로 값 목록을 만들어 값마다 `simulatePensionPortfolio()`를 따로 돌림(예: 1~12, step 1 → 12번 실행). 그래서 "단일 결과 화면"에서 "탭1과 같은 다중 조합 비교 화면"으로 구조가 통째로 바뀜: `lastPenResult`(단수) → `lastPenResults`(배열) + `lastPenBase`(간격과 무관하게 1개뿐이라 그대로), 결과표에 "표시" 체크박스 열 추가(`penVisible`, 기본은 기준+최고수익 조합만 켜짐 — 탭1 `setDefaultVisibility`와 동일한 발상), 연도별 성과표/거래이력/종목별요약은 "전략 선택" 드롭다운으로 어떤 조합을 볼지 고르는 방식(탭1의 `getCheckedLwOptions`/`populateLwYearlySelect`/`populateLwTradeLogSelect` 패턴을 `pen` 접두어로 그대로 복제 — `getCheckedPenOptions`/`populatePenYearlySelect`/`populatePenTradeLogSelect`).
   - 이 리팩터링 중 버그 2개 발견: ① 조합별 "거래횟수"가 항상 0으로 나왔음 — `statsFromCurveCustomBase`에 빈 tradeList(`[]`)를 넘겨서 `stats.trades`가 항상 0인데, 그걸 그대로 노출하고 있었음. `simulatePensionPortfolio()` 반환값 맨 끝에 `trades: tradeLog.length`로 덮어써서 고침(스프레드 순서상 `...stats` 뒤에 와야 0으로 다시 안 덮임). ② `computePenBase()`가 curve 배열만 반환해서, 새 "전략 선택" 드롭다운에서 기준(보유전략)을 고르면 종목별 요약이 빈 표로 나왔음 — `{curve, perStock, finalCash}` 형태로 다른 조합들과 모양을 맞춰서 반환하도록 수정.
   - 예전 저장 전략(`rebalanceMonths` 단일값)을 불러오면 `applyPenStrategySnapshot()`이 `rebalanceMin=rebalanceMax=그값, step=1`로 자동 변환 — 하위호환 마이그레이션 코드 필요 없이 그대로 동작함.

### 9. 탭3 "미국ETF비교" — 국내 ETF를 추종 미국 원본×환율로 환산해서 짧은 상장기간 보완
2026-09-03 추가. 실제 투자 대상인 국내 상장 ETF(TIGER 미국나스닥100/필라델피아반도체/배당다우존스, KINDEX KRX금현물, KODEX 200미국채혼합)는 상장일이 짧아서(759~3884거래일) 백테스트 신뢰도가 낮음 — 이 종목들이 추종하는 **미국 원본 ETF**(QQQ/SOXX/SCHD/GLD/IEF, Yahoo Finance `adjclose` = 배당 재투자 반영 수정종가) 값에 그날의 USD/KRW 환율을 곱해서 "만약 한국에 더 일찍 상장됐다면"에 해당하는 원화환산 시리즈를 대신 씀(최대 5,030거래일, 2003-12-01~).

- **데이터 출처**: `stock-signal-app`이 아니라 이 대화에서 직접 Yahoo Finance 차트 API(`query1.finance.yahoo.com/v8/finance/chart/{symbol}?period1=...&period2=...&interval=1d`)로 받아온 것 — `range=max`는 장기간 요청 시 자동으로 월봉으로 다운샘플링돼버려서(263개월치만 옴) 안 됨, 반드시 `period1`/`period2`(유닉스 타임스탬프)를 명시해서 청크(8년 단위)로 나눠 받아야 일봉 그대로 유지됨. `adjclose`가 없으면 `close`로 대체. 이 세션에서 로컬 Python의 `ssl` 모듈이 깨져있어서(`_ssl` DLL 로드 실패) `requests`로 바로 못 부름 — `curl` 서브프로세스로 우회해서 받음(다른 파이썬 스크립트 짤 때도 이 환경 제약 기억할 것).
- **별도 상수로 완전히 분리**: `US_ETF_DATA`(티커→`{dates, close}`)와 `USD_KRW_DATA`(`{dates, close}`)는 `PRESETS`와 전혀 다른 새 상수 — `PRESETS`는 탭1/탭2가 OHLCV 전체(시가/고가/저가/거래량)를 가정하고 쓰는데 이 데이터는 종가만 있어서, 같이 섞으면(예: `PRESET_INFO_LIST`에 추가) 탭1/탭2가 깨짐. `PEN_US_ETF_MAP`(국내 종목코드→미국 티커)로 탭3 전용 `getSelectedPenDatasets()`에서만 바꿔치기함.
  - **2026-09-05에 4개 추가**: 360750(TIGER 미국S&P500)→SPY, 283580(KODEX 차이나CSI300)→ASHR, 241180(TIGER 일본니케이225)→EWJ, 453870(TIGER 인도니프티50)→INDA. SPY/ASHR는 원본 지수가 정확히 일치(S&P500, CSI300)하는 강한 매칭이고, EWJ/INDA는 MSCI Japan/MSCI India를 추종해서 니케이225/니프티50과 지수 자체는 다르지만 "그 나라 시장" 대표 ETF로 흔히 쓰이는 근사 매칭임(사용자에게 신뢰도 구분해서 미리 안내함). TLT는 여전히 데이터만 있고 매핑은 안 함, EWY(한국시장)도 워크북엔 있지만 지금 보유종목 중 KOSPI200과는 추종지수가 달라서 매핑 안 함.
  - 이 4개는 새로 야후에서 받은 게 아니라 **이전 세션에서 이미 받아뒀지만 아직 앱에 연결 안 하고 있던 데이터**(스크래치패드의 `미국ETF원본_수정종가_환율.xlsx`, SPY/EWY/EWJ/INDA/ASHR/ACWI 시트가 이미 있었음)를 그제서야 꺼내 쓴 것 — 앞으로 추가로 궁금한 종목이 생기면 이 워크북에 이미 있는지부터 확인할 것(현재 EWY/ACWI가 아직 미사용으로 남아있음, ACWI는 "KODEX MSCI선진국"의 근사 후보지만 올컨트리(신흥국 포함)라 선진국 전용 지수와는 안 맞아서 보류함 — 더 정확한 대응은 VEA/URTH인데 아직 안 받음).
  - **대용량 데이터 삽입 스크립트를 짤 때 흔한 함정(2026-09-05에 겪음)**: `content.index(marker)`로 삽입 지점을 찾고 `content.rindex("};\n", 0, idx)`로 그 직전의 닫는 괄호를 찾는 방식을 썼는데, marker를 `"\nconst USD_KRW_DATA"`처럼 **선행 개행 문자를 포함해서** 잡으면, 그 개행 문자가 바로 앞 `"};\n"`의 개행과 **같은 문자**라서 `rindex(sub, 0, idx)`의 탐색범위(`idx` 미포함, 즉 그 개행 문자 자체가 범위 밖으로 밀려남)에서 그 "};\n"이 안 걸림 — Python은 그보다 훨씬 앞에 있는 엉뚱한 "};\n"(이 경우 `PRESETS` 객체의 닫는 괄호)를 대신 찾아서 데이터가 완전히 엉뚱한 위치에 삽입됐었음. **탐색범위를 `idx + 1`로 하거나, marker에서 선행 개행을 빼고 잡을 것.** 삽입 후엔 반드시 `US_ETF_DATA`의 top-level 키 목록을 정규식으로 뽑아서 새 티커가 실제로 그 안에 들어갔는지 확인하는 절차를 거칠 것(이번엔 그렇게 해서 바로 발견 → git checkout으로 되돌리고 재삽입함).
- **환산 방식**: `buildUsEtfKrwSeries(ticker)` — 미국 ETF와 환율 둘 다 값이 있는 날짜끼리 교집합(하나라도 없는 날은 계산 불가라 제외), `close = round(미국원본 × 그날환율)`. 티커별로 한 번 계산하면 캐시(`_usEtfKrwCache`)해서 재실행 시 다시 안 만듦.
- **체크박스 켜고 끌 때마다 기간 범위를 다시 계산**해야 함(`updatePenPeriodDefaults(false)` 재호출) — 미국 데이터가 국내보다 기간이 훨씬 길어서, 안 그러면 "겹치는 구간"이 옛날 국내 기준 그대로 남아있음.
- **매핑 없는 종목은 그대로**: 체크박스가 켜져 있어도 `PEN_US_ETF_MAP`에 없는 종목(예: KODEX 200)은 국내 데이터를 그대로 씀 — UI 노트(`penUsEtfNote`)에 어떤 종목이 환산됐고 어떤 종목이 대응 없이 그대로인지 항상 표시함.
- **검증**: 로컬에서 `getSelectedPenDatasets()`가 실제로 만들어낸 원화환산 종가가 `Math.round(미국종가 × 환율)`과 소수점까지 정확히 일치하는지 직접 대조 확인, 5종목·22년(2003~2026)치 백테스트 실행까지 확인함.

### 10. 탭3 "역변동성 가중" / "자산곡선 DD컷" 옵션 — 리밸런싱 스케줄러도 함께 교체
2026-09-04 추가. 사용자가 클로드 채팅에서 별도로 검토해둔 pseudocode를 그대로 옮겨서 체크박스 2개(`penInverseVolOn`/`penDdCutOn`)로 켜고 끌 수 있게 함. 탭3에만 적용(사용자가 "탭3에만 적용해주면돼 나머지탭말고"라고 범위를 명시적으로 한정) — 탭1/탭2/섹터ETF/상관관계분석은 변경 없음.

- **리밸런싱 시점 계산 방식 교체**: "8."에서 쓰던 `buildPeriodBoundariesGeneric`(월 개수로 균등 일수 분할, 실제 달력과 무관)을 이번에 새로 만든 `computeMonthlyFirstTradingDayIndices(dates)`로 바꿈 — `"YYYY-MM"` 문자열 prefix가 전날과 달라지는 지점을 그 달의 "첫 거래일"로 잡고(Date 객체/타임존 문제 없음), N개월 간격은 그 인덱스 목록에서 매 N번째 값만 취함. 사용자 pseudocode의 "이번 달의 첫 거래일" 요구사항과 정확히 일치시키기 위한 교체 — `buildPeriodBoundariesGeneric` 자체는 그대로 남아있고 연도별 통계 등 다른 용도에 계속 쓰임.
- **규칙A 역변동성 가중**: 리밸런싱일마다 자산별 연율화 변동성(최근 60거래일 로그수익률 표준편차×√252, 반드시 "어제까지" 데이터만 사용 — lookahead 없음)으로 목표비중을 나눈 뒤 재정규화. 변동성은 종목마다 **자기 자신의 가격 인덱스 기준**으로 60일치를 셈(종목별 상장일이 달라도 안전). 그날 리밸런싱 대상 종목 중 하나라도 60일치가 안 쌓였으면(전략 시작 초반) 역변동성 조정 없이 원래 목표비중 그대로 씀.
- **2026-09-11 버그 수정: 최초 매수(t=0)엔 규칙A가 항상 안 먹히던 문제.** `annualizedVol(e,t)`가 "어제" 인덱스를 구할 때 `dateMaps[e].get(masterDates[t-1])`로 먼저 찾았는데, t=0(최초 매수일)엔 `masterDates[-1]`이 아예 없어서(`undefined`) 그 종목이 실제로는 상장한 지 60거래일이 훨씬 지났어도 무조건 `null`을 반환 → `allHaveVol=false`가 되어 최초 매수는 항상 사용자가 입력한 목표비중 그대로 매수되고, 그 다음 리밸런싱부터만 역변동성이 적용되고 있었음(사용자가 "처음 살 때만 규칙A가 안 먹히는 것 같다"고 발견). **고침**: "어제" 인덱스를 `masterDates[t-1]`이 아니라 **오늘(`masterDates[t]`)의 그 종목 자체 인덱스에서 1을 뺀 값**으로 구하도록 바꿈(`idxToday = dateMaps[e].get(masterDates[t]); idx = idxToday - 1`) — `masterDates`엔 없어도 그 종목 자체 시계열(`datasets[e].close`, 보통 선택한 백테스트 기간보다 훨씬 이전 데이터까지 다 갖고 있음)엔 이미 60일치가 쌓여있을 수 있으므로, t=0이어도 구해짐. t>0인 일반 리밸런싱일엔 종목이 마스터 날짜와 동일한 거래일력을 쓰는 한(같은 거래소 상장 종목이면 항상 그러함) 이전과 완전히 같은 값이 나옴 - 합성 데이터로 t=0과 이후 매달 리밸런싱의 목표비중이 전부 동일한 비율(예: 0.977/0.023)로 일관되게 나오는 것으로 회귀 확인.
- **규칙B 자산곡선 DD컷**: 전략 자체 평가액 곡선(`curve`)이 어제까지 사상최고치(`runningPeak`) 대비 -7% 넘게 빠져 있으면, 그날 리밸런싱 목표비중 전체에 0.5배를 곱함 — 축소분은 매수하지 않고 그대로 현금으로 남음(현금 이자는 미반영, 사용자가 사전에 "빼도 결과 큰 영향 없다"고 확인). 두 규칙 동시 적용 시 순서는 역변동성 재정규화 → DD컷 축소.
- **검증**: 합성 데이터로 각각 독립 테스트. 역변동성은 변동성 비율 20:1인 자산 2개로 예산배분이 대략 20:1로 갈리는지 확인. DD컷은 -20% 단일크래시 시나리오로 다음 리밸런싱일에 정확히 보유량 절반이 매도되는지 확인 — 그 이후 매달 소액의 추가 매도가 계속 찍혀서 처음엔 버그로 의심됐으나, 현금엔 이자가 안 붙고 주식만 계속 소폭 상승하는 합성 시나리오라 "현금 50% 유지" 목표를 매달 다시 맞추려고 몇 주씩 트리밍하는 게 정상 동작임을 확인함. 실데이터(탭3 미국ETF환산 5종목, 22년치, 두 규칙 모두 ON)로도 에러 없이 끝까지 실행되고 결과표가 정상 표시되는 것까지 확인.
- **결과표에 DD컷횟수/로그자산R² 컬럼 추가(2026-09-04 같은 날 추가 요청)**: `doRebalance(t)` 안에서 `shrinkFactor`가 실제로 0.5가 될 때마다 `ddCutCount`를 증가시켜서 `simulatePensionPortfolio()` 반환값에 포함(리밸런싱 자체가 없는 `computePenBase()`쪽 "기준" 행은 개념상 항상 0으로 고정). `logR2`는 새로 만든 게 아니라 `statsFromCurveCustomBase()`가 이미 내부적으로 `computeLogAssetR2()`(탭1/탭2에서 쓰던 것과 동일한 로그자산R² 유틸)를 계산해서 반환값에 포함시키고 있었는데, 탭3 결과표(`renderPenResults()`)에서만 그동안 렌더링을 안 하고 있었던 것 — 새 유틸 없이 `cols`/`rowHtml`에 컬럼 2개만 추가.
- **거래이력 표에 "목표비중" 컬럼 추가(2026-09-04)**: 규칙A(역변동성 가중)를 켜면 매달 리밸런싱마다 종목별 목표비중이 달라지는데, 거래이력만 봐서는 그 비중을 알 수 없었음 — `doRebalance(t)`의 매도/매수 `tradeLog.push()` 양쪽에 그 시점 최종 적용비중(`normWeight[e]*shrinkFactor`, 규칙A/B 다 반영된 값)을 `ratio` 필드로 같이 기록하고, 거래이력 표/엑셀 내보내기에 퍼센트로 표시. 실데이터로 같은 종목의 목표비중이 33.3%→21.9%/35.5%/42.6%처럼 달마다 바뀌는 것으로 확인(초기엔 종목별 상장일이 달라 3종목만 available해서 33.3%씩 균등으로 시작).
- **"리밸런싱 거래횟수" 컬럼이 리밸런싱 "횟수"가 아니라 "거래 건수"였던 혼란(2026-09-04, 사용자가 "3.2년 월 1회면 38~39번일텐데 왜 187번이냐"고 물어서 발견)**: `trades`(=`tradeLog.length`)는 리밸런싱 한 번에 종목별로 나오는 개별 매수/매도 건을 전부 합친 값이라, 종목 5개면 리밸런싱 1회당 최대 5건이 찍혀서 "몇 번 리밸런싱했는지"로 착각하기 쉬웠음(버그 아님, 라벨 문제). **해결**: `rebalanceSet.size`(실제 리밸런싱이 실행된 횟수, `computePenBase()` 쪽 "기준" 행은 리밸런싱 자체가 없어 항상 0)를 `rebalanceCount`로 새로 반환·표시하는 컬럼("리밸런싱 횟수")을 추가하고, 기존 `trades` 컬럼 라벨은 "매매 건수"로 바꿔서 구분함. 실데이터(781거래일≈3.1년, 종목 5개, 월 1회)로 리밸런싱 횟수 37 / 매매 건수 174(≈37×4.7)로 확인 — 사용자가 예상한 38~39번과 부합.

### 11. 탭3 "미국ETF비교" 데이터도 "전체 종목 최신화"에 포함 + 종목별 매핑 표시
2026-09-04 추가. "9."의 `US_ETF_DATA`/`USD_KRW_DATA`는 그때까지 이 대화에서 dev-time에 한 번 받아서 박아넣은 스냅샷이라, 시간이 지나면 국내 KIS 데이터(항상 최신화됨)와 날짜가 안 맞아 뒤처지는 문제가 있었음 — 기존 "전체 종목 최신화" 버튼을 누르면 이 데이터도 같이 최신 날짜까지 갱신되도록 함.

- **서버 프록시 신설(`api/usetf.py`, `/api/usetf`)**: 야후 파이낸스 차트 API를 브라우저에서 직접 fetch하면 CORS로 막혀서(Access-Control-Allow-Origin 없음), `api/history.py`(KIS 프록시)와 같은 이유로 서버 함수를 거침. `?symbols=QQQ,SOXX,...,KRW=X&start=YYYYMMDD&end=YYYYMMDD`로 여러 심볼을 한 번에 배치 조회(marketcap_batch와 같은 이유 — 심볼마다 왕복하면 느림), 심볼별 실패는 `errors`에 개별 기록하고 나머지는 계속 진행. USD/KRW 환율의 야후 심볼은 `"KRW=X"`.
- **클라이언트 쪽 갱신(`updateUsEtfAndFxData()`)**: `US_ETF_DATA`의 각 티커 + `USD_KRW_DATA`마다 저장된 마지막 날짜 다음날부터 오늘까지만 요청(전부 최신이면 네트워크 호출 자체를 생략). 받아온 결과는 `mergeSeriesData()`(날짜 키로 병합 후 정렬, `mergeStockData`의 단일필드 버전)로 기존 배열에 병합, 환산 캐시(`_usEtfKrwCache`)를 비워서 다음 백테스트 때 새 데이터로 다시 계산되게 함. **"전체 종목 최신화"(`dataUpdateAllBtn`) 핸들러 끝에서 KIS 데이터 최신화 뒤에 이어서 호출**됨(실패해도 KIS 쪽 최신화 결과에는 영향 없게 별도 try/catch).
- **저장 위치는 의도적으로 PRESETS와 다르게 함**: `US_ETF_DATA`/`USD_KRW_DATA`는 Vercel Blob(`/api/presets`, 여러 기기 동기화용)에 안 얹고 이 기기의 `localStorage`(키 `pen_us_etf_override_v1`)에만 저장함(`saveUsEtfOverrides()`/`loadUsEtfOverrides()`) — 참고용 보조 데이터라 기기마다 따로 최신화해도 문제없고, 이미 900KB+인 이 데이터를 PRESETS 오버라이드 Blob에 매번 함께 얹으면 그 저장·전송 비용만 커짐. 앞으로 여러 기기 동기화가 필요해지면 그때 "1."의 병합+tombstone 패턴을 이쪽에도 적용할 것.
- **날짜 하루 더하기는 반드시 `ymdAddDays()`(로컬 연/월/일 기준)로**: 처음 짤 때 `Date.toISOString()`으로 다음날을 계산했다가, KST(UTC+9)에서 자정 근처 날짜가 하루 밀리는 버그를 겪음(로컬 자정을 UTC로 변환하면 전날 오후가 돼서 `.slice(0,10)`이 하루 전 날짜를 돌려줌) — `computeMonthlyFirstTradingDayIndices`가 Date 객체 대신 문자열 비교를 쓰는 것과 같은 부류의 함정. 기존에 있던 `todayYmd()`/`ymdAddDays()`(로컬 `getFullYear`/`getMonth`/`getDate` 기반)로 바꿔서 해결.
- **종목 선택 체크리스트에 매핑 표시**: `renderPenPresetChecklist()`에서 `PEN_US_ETF_MAP`에 있는 종목마다 체크박스 아래에 작은 글씨로 "↳ 추종 미국ETF: QQQ" 식으로 표시(매핑 없는 종목은 표시 없음) — 사용자가 "미국ETF비교"를 켜기 전에도 어떤 종목이 환산 대상인지 미리 알 수 있게 함.
- **검증**: `fetch`를 몽키패치해서 `/api/usetf` 응답을 목업으로 대체 후 `updateUsEtfAndFxData()` 직접 호출 — 마지막 저장일 다음날부터(타임존 버그 수정 후 정확한 날짜로) 오늘까지만 요청하는지, 병합 후 배열 길이/마지막 날짜가 맞는지, `localStorage` 저장 후 새로고침해도 유지되는지, 이미 최신이면 네트워크 호출 자체가 생략되는지 확인. `api/usetf.py`는 `python -m py_compile`로 문법 검증(로컬 Python `ssl` 모듈이 깨져있어(위 "9." 참고) `requests`로 실제 야후 호출까지는 로컬에서 테스트 못 함 — Vercel 배포 환경에서는 `api/history.py`가 이미 같은 `requests` 패턴으로 정상 동작 중이라 문제없을 것으로 판단).

### 12. 매매신호 앱(stock-signal-app) 연동 — 이 repo에는 없음, stock-signal-app 쪽에 있음 (2026-09-06)
처음엔 이 repo(탭2/탭3)에 "매매신호 앱에 적용" 버튼을 만들어서, 저장된 전략을 stock-signal-app으로 **밀어넣는(push)** 방식으로 구현했었음(`pushToSignalApp()`, 연동 URL/키를 이 기기 localStorage에 저장, 버튼 클릭 시 `POST stock-signal-app의 /api/sync_config`). 그런데 사용자가 원했던 그림은 반대 방향이었음 — "여기서 버튼 누르는" 게 아니라 **stock-signal-app에서 저장된 전략 목록(A/B/C)을 보고 직접 골라 적용**하는 것. 그래서 이 push 구현은 전부 되돌리고(커밋으로 추가했다가 같은 날 제거함) stock-signal-app 쪽에 pull(당겨오기) 방식으로 새로 만듦.

**이 repo가 pull을 지원하기 위해 갖고 있어야 하는 건 딱 하나 — `GET /api/presets`가 이미 인증 없이 `mvStrategyPresets`/`penStrategyPresets`를 전부 돌려주고 있다는 것**(원래 여러 기기 동기화용으로 만들어둔 것, 위 "1." 참고) — stock-signal-app이 이 엔드포인트를 직접 GET해서 저장된 전략 이름 목록을 가져가고, 사용자가 그중 하나를 그쪽 화면에서 고름. **이 repo 쪽엔 추가로 만들 게 없음** — 이미 있는 공개 조회 엔드포인트를 상대방이 읽어가는 것뿐이라, 이 repo는 stock-signal-app의 존재를 몰라도 됨(단방향 의존성: stock-signal-app → 이 repo, 반대 방향 없음). 자세한 흐름은 stock-signal-app repo의 CLAUDE.md "backtest-tool-app 연동" 참고.

### 13. "전체 종목 최신화" 자동 실행 (Vercel Cron, 평일 15:40 KST) — `api/cron_update_all.py`
기존 "전체 종목 최신화" 버튼(index.html)은 전부 브라우저 JS라 사람이 직접 눌러야만 실행됨. 매일 장 마감 후 자동으로 돌게 해달라는 요청으로, 같은 일(가격 데이터 증분 갱신 + 시가총액 갱신)을 하는 서버 함수 `api/cron_update_all.py`를 새로 만들고 `vercel.json`의 `crons`에 등록함(`"schedule": "40 6 * * 1-5"` — UTC 06:40 = KST 15:40, 월~금만).

- **"미국ETF비교" 데이터(`api/usetf.py`)는 자동화 대상에서 뺐음** — index.html 주석에 있듯 그 데이터는 애초에 **기기별 localStorage에만** 저장되는 보조 캐시라(위 "9."/"11." 참고), 서버에서 갱신해봤자 각 브라우저에 반영될 방법이 없음. 그건 여전히 사람이 버튼을 눌러야 갱신됨.
- **새 종목(가격 데이터가 아예 없는 코드)은 건너뜀** — 첫 백필은 수년치라 시간 예산을 넘기기 쉽고, 애초에 새 종목 추가는 사용자가 "새 종목 추가"로 직접 하는 흐름이라 자동화 범위 밖(응답의 `priceNoData`에 나열됨).
- **KIS API 호출 로직은 `api/history.py`와 별개로 이 파일에 통째로 복붙**(이 프로젝트의 "Vercel Python 함수끼리 서로 import 안 함" 관례 유지). `BASE_PRESET_INFO`(31개 기본 종목 code/name)도 index.html의 `PRESET_INFO_LIST`/`BASE_PRESET_CODES`를 그대로 복사해둔 것 — **index.html에서 기본 내장 종목을 추가/삭제하면 `api/cron_update_all.py`의 `BASE_PRESET_INFO`도 같이 고쳐야 함**(자동 동기화 없음, 수동 관리).
- **시간 예산**: Vercel 함수 기본 실행시간 제한(과거엔 10초였음, 위 "usetf.py" 주석 참고)을 이 함수만 `vercel.json`의 `"config": {"maxDuration": 60}`로 60초로 늘림(legacy `builds` 포맷에서도 build entry별 `config.maxDuration`은 지원됨). Cron은 Vercel Hobby 플랜에서 **하루 1번만** 허용되기 때문에(여러 번 나눠 재호출하는 체이닝 불가), 시간 예산을 넘기면 처리하다 만 상태를 그대로 Blob에 저장하고 조용히 끝냄(`priceTimedOut`/`capTimedOut`에 나열) — 못 끝낸 나머지는 다음날 "아직 오늘자가 아닌 종목"으로 자연스럽게 다시 잡혀서 이어짐. 하루 정도 갱신이 늦어지는 것뿐이라 개인용 앱에서 문제없다고 보고, 재귀/체이닝처럼 복잡하고 깨지기 쉬운 방식은 일부러 안 씀.
- **인증**: Vercel이 `CRON_SECRET` 환경변수를 보고 cron 트리거 요청에 자동으로 `Authorization: Bearer <CRON_SECRET>` 헤더를 붙여줌 - 이 값이 프로젝트에 없으면 이 함수는 무조건 401을 돌려줌(기본값이 "허용"이 아니라 "거부"). **Vercel 프로젝트 Settings → Environment Variables에 `CRON_SECRET`을 값 아무거나(랜덤 문자열)로 추가해야 실제로 동작함** — 안 하면 매일 401만 찍히고 아무 갱신도 안 일어남.
- **검증**: `merge_price_data()`(날짜 기준 병합, index.html의 `mergeStockData()`와 동일 동작), `run_update()` 전체 흐름(이미 최신인 코드 skip / 하루 뒤처진 코드만 fetch / 데이터 없는 코드 skip / 삭제된 코드 완전 제외 / market cap 배치 / Blob 저장)을 `vercel_blob`·KIS 호출을 목업으로 대체해서 단위 테스트함, 데드라인을 과거로 줘서 "시간 초과해도 크래시 없이 부분완료로 끝나는지"도 확인함. **실제 Vercel cron 트리거 자체는 로컬에서 재현 불가** — 배포 후 Vercel 대시보드 Settings → Cron Jobs에서 실행 로그를 봐야 정상 동작(특히 60초 안에 다 끝나는지, `maxDuration` 설정이 실제로 반영됐는지)을 확인할 수 있음.

## 주요 변경 이력 (최근 → 과거, 상세 배경은 `git log`로 각 커밋 메시지 확인)
- `cec8208` 탭3에 "미국ETF비교" 체크박스 추가(위 "9." 참고) — 국내 상장 기간 짧은 종목을 추종 미국 ETF×USD/KRW 환율 환산값으로 대체해서 훨씬 긴 기간 백테스트 가능. `US_ETF_DATA`/`USD_KRW_DATA`/`PEN_US_ETF_MAP` 새 상수 추가(파일 크기 약 1MB 증가, `PRESETS`와 분리 관리).
- `cb23275` 탭3(개인연금 보유전략) 후속 수정 2건(위 "8." 하단 참고) — ①종목 선택 시 비중 자동 균등분배(100/N%), ②리밸런싱 간격을 탭1 k값처럼 최소/최대/step 그리드서치로 변경(`lastPenResult` 단수 → `lastPenResults` 배열, 탭1의 다중조합 비교 UI 패턴을 `pen` 접두어로 복제). 겸사겸사 조합별 거래횟수 항상 0으로 나오던 버그, 기준(보유전략) 선택 시 종목별요약 빈 표로 나오던 버그도 수정.
- `d0bbd68` 탭3 "개인연금 보유전략" 신설(위 "8." 참고) — 여러 종목을 목표비중대로 매수/보유하다가 N개월마다 리밸런싱하는 정적 자산배분 전략, 화면은 탭2와 동일하게 구성. 기존 섹터ETF(탭3→4)/상관관계분석(탭4→5) 표시 번호만 하나씩 밀림(내부 id는 안 바꿈).
- `539e36d` 탭2(여러종목투자) 종목별 설정 테이블에 슬리피지(%) 컬럼 추가 — 탭1/탭2의 기존 전역 슬리피지(틱/%)는 모든 종목에 똑같이 목표가 자체를 밀어버리는 "목표가 추가수정" 방식인데, stock-signal-app 실거래 대조 결과 종목마다 목표가-체결가 괴리 비율이 다르게 나와서 이걸로는 재현이 안 됐음. 이 새 값은 목표가는 그대로 두고 **체결 시 수수료처럼 종목별로 추가 비용만 뺌**(`simulateMvPortfolio()`에서 `fee` 대신 `fee + 종목별 slipPct`) — 탭1엔 아직 없음(탭2만 요청받음).
- `2c0df75` 탭1 "슬리피지 적용"을 "목표가 추가수정(x)"으로 명칭 변경(실제로는 시장 슬리피지 시뮬레이션이 아니라 k로 계산한 목표가에 틱/%를 추가로 얹는 기능이라 사용자가 정정) + 매수/매도 x값을 단일 입력에서 최소/최대/step 범위 입력으로 바꿔서 k값 그리드서치처럼 여러 x값도 한 번에 비교 가능하게 함(`lwSlipBuyTicks`/`lwSlipSellTicks` 단일 필드 → `lwSlipBuyMin/Max`, `lwSlipSellMin/Max`, `lwSlipStep`로 교체, `runLw()`의 combos 생성에 x조합도 포함). 체크박스 꺼져있으면 기존과 동일하게 x=0 단일 조합으로만 동작(내부 함수 `larryWilliams()`의 파라미터명/`slippageOn` 등은 안 바꿈, UI 라벨만 변경).
- `3d69397` 탭2(여러종목투자) 배정예산 계산 시점 버그 수정 — 사용자가 실제로는 매수/매도를 전날 밤 예약주문으로 미리 걸어두므로, 배정예산은 "그날 매도로 들어올 돈"이 반영되기 전(전날 마감 시점)의 현금 기준이어야 하는데, `simulateMvPortfolio()`가 그날 매도 처리 후의 현금으로 나누고 있었음 — 신호 없는 종목의 유휴 현금이 그날 우연히 매수 후보가 적으면 그쪽으로 몰려서 배정예산이 정상 슬롯(총자금÷종목수) 대비 훨씬 커지는 문제가 있었음(예: 210만원이어야 할 배정예산이 565만원으로 튐, 실사용자가 거래이력에서 직접 발견). `cashBeforeToday`(그날 매도 처리 전 현금 스냅샷)를 배정예산 계산에 쓰도록 수정, 합성 2종목 시나리오로 수정 전/후 값 차이(990,478 vs 500,095) 확인 후 커밋.
- `35a156b` 탭1(변동성 돌파 전략) 기본 k값 범위 변경 — MA20 이상/이하 둘 다 매수 0.1~1, 매도 0~1, step 0.1로 통일(이전엔 이상 구간 매수/매도 0.1~0.9 step 0.05, 이하 구간은 0.45/0.1 고정값이었음)
- `9bfc298` 탭2(여러종목투자) 기간 버튼을 "전체기간으로 설정"(선택 종목 중 가장 오래된 상장일 기준, 합집합)과 "겹쳐진기간으로 설정"(선택 종목 전부에 데이터가 있는 교집합, 기존 "전체 기간으로 초기화" 버튼의 실제 동작) 두 개로 분리 — 기존 버튼 이름이 실제 동작(교집합)과 안 맞았음. 체크박스 변경 등 자동 갱신은 기존처럼 겹치는 구간을 기본값으로 계속 사용.
- `c297196` 탭1 "안정성 지표 계산 주기(개월)" 입력 제거(탭2/여러종목투자에서 이미 제거한 것과 동일 패턴, 내부 계산은 12개월 고정 유지) + 하락장(MA20↓) k값 입력을 상승장 k값 바로 아래로 이동 + 슬리피지 적용 블록을 안정성 지표가 있던 자리로 이동(K매수/매도k → 슬리피지 → RSI기간 순서로 붙게 재배치)
- `f0530f8` tombstone 세 번째 변종 수정 — 다른 기기의 오래된 로컬 삭제기록이 서버 재동기화로도 안 풀리던 버그(위 "1." 참고)
- `7961040` "새 종목 추가"로 예전에 삭제했던 코드를 다시 추가해도 새로고침하면 또 사라지던 버그 수정(`deletedCodes` tombstone에서 빼는 로직 누락, 위 "1." 참고)
- `63cd6e2` "새 종목 추가"가 한 번에 여러 종목을 순서대로 처리하도록 변경(종목코드/종목명 입력을 "코드,종목명" 한 줄씩 입력하는 textarea로 교체). 시가총액은 새로 추가된 종목들을 모아서 `marketcap_batch`로 한 번에 조회.
- `f7dc73a` MDD 차트 라인을 항상 빨간색(`#e5484d`)으로 통일 + 과거 시세를 미수정주가(원주가)로 전환(위 "5." 참고)
- `3c4bb32` 탭2에 MDD 차트 토글 + 슬리피지(틱/%) 설정 추가, "안정성 지표 계산 주기(개월)" 입력 제거(내부 고정 12개월), 탭1·탭2 공통으로 "정수 주 단위로 매매" 체크박스 제거하고 항상 정수 주로만 매매하도록 단순화(분수 주 매매 코드 경로 삭제)
- `a47fefa` 탭2 공유 현금 풀 모델의 버그 수정 — 오늘 매도한 종목이 같은 날 바로 재매수돼서 하루에 2건 찍히던 문제, `soldToday` 제외 처리 추가
- `fa307c4` 탭2를 목표비중 강제 리밸런싱 → 공유 현금 풀 모델로 전면 개편(위 "4." 참고), 리밸런싱 이력을 배정예산 포함 거래 이력으로 교체, 종목별 초기투자금/목표비중 입력 제거
- `0e5d6c9` 탭2 차트에 로그 스케일 토글 추가
- `8e15394` 탭2 "전체 포트폴리오" 거래비율이 284%처럼 왜곡되던 버그 수정(종목 합산 거래횟수 ÷ 거래일수만 하던 걸 ÷(거래일수×종목수)로) + 종목별 요약에 거래비율(%) 표시로 변경
- `2862349` 시가총액 조회를 배치 엔드포인트로 묶어서 KIS 토큰 재발급 제한 에러 해결
- `8bd99fe` 탭 번호 1,1-1,2,3→1,2,3,4로 변경 + 탭2에 전략(종목선택+k값) 저장/불러오기 기능 추가 + 삭제 tombstone 버그 수정
- `7c954a1` 구 탭4(연금)·탭5(자산배분) 완전 삭제
- `718afec` 탭1 결과표에 컬럼별(11개) 범위 필터 추가
- `ed10ba1`~`d337073` Vercel Blob 저장 안정화 연쇄 수정: `allowOverwrite` 누락으로 두 번째 저장부터 계속 실패하던 근본 원인 발견, 병합 로직으로 전환, "저장 전 동기화" 전 동작에 통일, localStorage 용량초과가 서버저장 막던 문제 분리, KIS 연결 끊김 재시도(`_get_with_retry`)
- `ab9645b`~`faeba1f` 초기투자금 입력 가능화(탭1/탭2) + 정수 주 단위 매매 옵션
- `fb2d0de`~`f6cc59e` 종목 삭제 기능, 탭1 드롭다운 시가총액 표시/정렬
- `15af964` 여러 기기 동기화(Vercel Blob) 최초 도입
- 그 이전: KIS API로 시세 갱신/신규종목 추가, PWA 매니페스트, 최초 배포

세부 배경(왜 이렇게 결정했는지, 겪었던 버그의 증상 등)은 auto-memory의 `project-backtest-tool-app` 메모리에 더 자세히 남아있음 — 새 세션에서 필요하면 그쪽도 참고.
